import React from 'react';
import StubApp from '../shared/StubApp.jsx';

export default function WhiteboardApp({ isDark }) {
  return <StubApp isDark={isDark} appLabel="Quadro Branco" appColor="#7630CA" />;
}