import React from 'react';
import StubApp from '../shared/StubApp.jsx';

export default function SlidesApp({ isDark }) {
  return <StubApp isDark={isDark} appLabel="Apresentações" appColor="#FB6704" />;
}