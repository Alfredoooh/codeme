import React from 'react';
import StubApp from '../shared/StubApp.jsx';

export default function SheetsApp({ isDark }) {
  return <StubApp isDark={isDark} appLabel="Folha de Cálculo" appColor="#23A63F" />;
}