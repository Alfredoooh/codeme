import React from 'react';
import StubApp from '../shared/StubApp.jsx';

export default function AuthApp({ isDark }) {
  return <StubApp isDark={isDark} appLabel="Autenticação" appColor="#1F2937" />;
}