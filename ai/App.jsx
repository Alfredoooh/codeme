import React from 'react';
import StubApp from '../shared/StubApp.jsx';

// Stub temporário — a app 'ai' ainda não foi reconstruída nesta base
// React. Ver shared/StubApp.jsx para o ecrã "Em breve" partilhado por
// todos os stubs, e ver o projeto Svelte para a implementação de
// referência que será portada para aqui.
export default function AiApp({ isDark }) {
  return <StubApp isDark={isDark} appLabel="Assistente de IA" appColor="#862CD4" />;
}